<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewModels="clr-namespace:Solution.DesktopApp.ViewModels"
             xmlns:models="clr-namespace:Solution.Core.Models;assembly=Solution.Core"
             xmlns:converters="clr-namespace:Solution.ValidationLibrary.Converters;assembly=Solution.ValidationLibrary"
             xmlns:behaviors="clr-namespace:Solution.DesktopApp.Behaviors"
             x:Class="Solution.DesktopApp.Views.ManageCompetitionView"
             x:DataType="viewModels:ManageCompetitionViewModel">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior Command="{Binding AppearingCommand}" EventName="Appearing"/>
        <toolkit:EventToCommandBehavior Command="{Binding DisappearingCommand}" EventName="Disappearing"/>
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:FirstValidationErrorConverter x:Key="FirstValidationErrorConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <VerticalStackLayout Grid.Column="0"
                             Style="{StaticResource FormLineContainer}"
                             Margin="40">
            <Border Stroke="{AppThemeBinding Light=Black, Dark=White}"
                    StrokeThickness="2"
                    Background="Transparent"
                    Padding="0"
                    HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <Image Source="{Binding Image}" />
            </Border>
            <Button Text="Please select an image"
                    Margin="0,5"
                    Command="{Binding ImageSelectCommand}" />
        </VerticalStackLayout>
        <VerticalStackLayout Grid.Column="1">
            <Label Text="{Binding Title}" Style="{StaticResource PageTitle}" />

            <VerticalStackLayout Style="{StaticResource FormLineContainer}">
                <Label Text="Participating teams"/>
                <CollectionView ItemsSource="{Binding Teams}" SelectionMode="Multiple">
                    <CollectionView.Behaviors>
                        <toolkit:EventToCommandBehavior EventName="SelectionChanged" Command="{Binding TeamsSelectionChangedCommand}" />
                    </CollectionView.Behaviors>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Label Text="{Binding Name}" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource FormLineContainer}">

                <Label Text="Date" />
                <DatePicker Date="{Binding Date.Value}">
                    <DatePicker.Behaviors>
                        <toolkit:EventToCommandBehavior EventName="DateSelected" Command="{Binding DateIndexChangedCommand}" />
                    </DatePicker.Behaviors>
                </DatePicker>
                <Label Text="{Binding Date.Errors, Converter={StaticResource FirstValidationErrorConverter}}"
                       Style="{StaticResource ValidationErrorLabelStyle}"/>
                

            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource FormLineContainer}">
                <Label Text="Location" />
                <Picker ItemsSource="{Binding Locations}">
                    <Picker.Behaviors>
                        <toolkit:EventToCommandBehavior EventName="SelectedIndexChanged" Command="{Binding LocationIndexChangedCommand}" />
                    </Picker.Behaviors>
                </Picker>
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource FormLineContainer}">
                <Label Text="Participating teams"/>
                <Picker ItemsSource="{Binding Teams}">
                    <Picker.Behaviors>
                        <toolkit:EventToCommandBehavior EventName="SelectedIndexChanged" Command="{Binding TeamIndexChangedCommand}" />
                    </Picker.Behaviors>
                </Picker>
            </VerticalStackLayout>

            <VerticalStackLayout Style="{StaticResource FormLineContainer}">
                <Button Text="Submit" Command="{Binding SubmitCommand}" WidthRequest="250"/>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </Grid>
</ContentPage>